# 独立复审 · ChatAgent 跨会话记忆「自我认知」补全（gate-③）

> 复审对象：`chat_agent.py` 的 `CHAT_SYSTEM_PROMPT` 纯文本增补（能力条 + `【你的长期记忆（跨会话知识库）】` 小节）。
> 前序根因修复：`5c138b2`（kb_store 关键词兜底召回）。本变更无可执行逻辑改动。
> 复审方式：仅读码，未修改任何文件。

## 裁决（verdict）

**PASS_WITH_NOTES**

纯文本 prompt 增补与代码事实一致，诚实边界描述准确，无缺陷引入。仅一处措辞精度建议（F3，MINOR），不影响放行。

## 发现（findings）

### F1 — 注入标题字符串与 prompt 描述一致（PASS）
- 严重度：NOTE
- prompt 的能力条（行 26）与记忆小节（行 80）均描述以「你之前积累的相关知识」注入。
- 代码 `chat_agent.py:380` 实际注入头为 `【你之前积累的相关知识（供参考，须自行核实时效性）】`，被引用字符串是其忠实子串。
- 结论：描述与实现一致，无串号。

### F2 — 召回机制描述与代码事实吻合（PASS）
- 严重度：NOTE
- prompt 诚实边界（行 82）称「召回当前为关键词匹配（嵌入模型暂不可用，走中文 bigram + 英文词匹配）」。
- 代码 `kb_store.py:53` `embed()` 对任何异常（含 402/429/不可达）一律 `return None`；`kb_retrieve`（`kb_store.py:173`）在 `vecs` 为空或向量列空时落 `_lexical_retrieve`（`kb_store.py:137`），即 `content ILIKE / title ILIKE` 的 bigram+英文词 OR，按命中 token 数排序。
- 结论：prompt 的「关键词、非语义、中文 bigram+英文词」表述与 live 代码路径精确对应；未过度声称具体 HTTP 原因（402 仅在 VERIFICATION 文档中交代），诚实边界未越界。

### F3 — 「每轮对话前…注入」措辞略强于实现（MINOR）
- 严重度：MINOR
- 记忆小节（行 80）写「每轮对话前，系统按用户问题从知识库召回…注入本系统提示」。
- 实际 `chat_agent.py:373` 为 `if hits:` 条件注入；无命中时 `kb_ctx=""`，该标题不会出现在 system 中。能力条（行 26）表述为「回答相关问题时…自动召回（…注入），供你参考」已隐含条件命中，更准确。
- 建议：将行 80 的「每轮对话前…注入本系统提示」弱化为「命中相关条目时注入本系统提示（标题为「你之前积累的相关知识」）」，与本变更自带的「诚实」主题一致；非阻塞。

### F4 — 自动沉淀 vs 「记住：」草稿通道区分正确（PASS）
- 严重度：NOTE
- prompt（行 81）称自动沉淀：工具取证实质回答后写入知识库；「记住：」是另一条需管理员采纳的草稿通道。
- 代码：`chat_agent.py:452-454` 在 `intent=="chat"` 且任一工具 `ok` 时调 `chat_learn_write`（自动）；`chat_agent.py:351-362` 将 `记住：` 路由到 `propose_lesson`（草稿，待 accept）。两者机制与边界描述一致。

### F5 — 字符串定界与转义完整性（PASS）
- 严重度：NOTE
- `CHAT_SYSTEM_PROMPT` 为普通三引号 `"""…"""`（无 `f` 前缀），新增内容位于同一字符串内，内部无 `"""` 断串，无 `{…}` 花括号。
- 结论：无 f-string 解析风险，花括号为字面安全；等价于 `py_compile` 通过，缺陷引入概率≈0。

### F6 — 变更范围干净，无逻辑副作用（PASS）
- 严重度：NOTE
- 全文仅 `chat_agent.py` 一处 prompt 字符串内新增文本（行 26 能力条、行 79-83 记忆小节），`step()` / `kb_retrieve` / `chat_learn_write` 等可执行逻辑均未触碰（对照 `5c138b2` 与根因）。无引入回归风险。

## 一致性小结

| 检查项 | 结果 |
|---|---|
| prompt 所称「确实拥有跨会话记忆」↔ `kb_retrieve` 已接线并注入 | 一致（F1/F4） |
| 诚实边界（关键词、非语义、bigram+英文词）↔ 代码降级链 | 一致（F2） |
| 自动沉淀 / 「记住：」草稿 双通道 ↔ 代码 | 一致（F4） |
| 标题字符串 | 一致（F1） |
| 定界符 / 转义 | 安全（F5） |
| 缺陷引入 | 无（F6） |

<!-- reviewed-by: independent-subagent (gate-③) -->
