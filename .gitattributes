# TD-006：强制关键文本文件以 LF 入库并在 checkout 时保持 LF。
# 背景：宿主 core.autocrlf=true，Windows 下干净 clone/checkout 会把 .sh 转成 CRLF，
#       Dockerfile 的 COPY 直接把 CRLF 脚本送进 Linux 容器 → /bin/sh^M: bad interpreter。
# 实测证据：git checkout-index 产物 CRLF=10（2026-09-07）。
# 说明：.gitattributes 治本（Git 层）；Dockerfile 内 `sed -i 's/\r$//'` 为运行时兜底（拷贝/归档场景）。

# Shell 脚本：必须 LF
*.sh text eol=lf

# 容器与 Web 服务器配置：必须 LF
Dockerfile text eol=lf
*.dockerfile text eol=lf
*.conf text eol=lf

# 编排/依赖清单：必须 LF（YAML 对 BOM/CRLF 敏感）
*.yml text eol=lf
*.yaml text eol=lf
requirements.txt text eol=lf

# 源码与文档：统一 LF
*.py text eol=lf
*.ts text eol=lf
*.vue text eol=lf
*.md text eol=lf
*.json text eol=lf

# 二进制：禁止任何换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.svg text eol=lf
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
