# UI 视觉与信息架构同步设计（Accio Work 风格）

> 状态：设计稿，待确认后实施  
> 依据：用户提供的 6 张 Accio Work 截图（zh.accio.com /work/app）与当前 deployed UI（http://localhost:18080）实测对比

---

## 1. 核心差异（实测）

| 维度 | Accio Work 模板 | 当前 report-agent-team UI |
|---|---|---|
| 产品定位 | 通用 AI Agent 工作平台（电商/办公/设计/建站/采购） | 研报协作平台（报告生成、审核、推送） |
| 左侧导航 | 新任务、智能体、插件、消息渠道、下载桌面端 | 研报入口、模板选择、提交任务、智能体、数据源、技能库、推送渠道、设置 |
| 首页 | 聊天对话框：Agent 头像、输入框、插件/智能体切换、场景快捷标签 | 研报表单：标题、大输入框、模板 chip、智能体组合、生成按钮 |
| 智能体市场 | 圆角头像卡片、名称+描述+角色标签、对话/安装按钮 | 方正卡片、字母头像、内置标签、配置/对话/删除 |
| 数据源/插件 | 按分类分区（资源与选品、市场调研与分析…）、卡片带+启用 | 按表格/网格、启用/停用/连接 |
| 消息渠道 | 独立大卡片、展示用户/群聊/待处理统计、配对模式/设置机器人 | 网格小卡片、端点输入、启用/测试/删除 |
| 用户区 | 左下角头像+升级按钮 | 无用户区，右上角调试开关 |

---

## 2. 目标

在不改变后端接口与引擎行为的前提下，把前端视觉与信息架构向 Accio Work 对齐，使其看起来像一个“Agent 平台”而不是“研报后台”。

**保留的硬约束**：
- UI 必须是 controller：配置改动真实回写 `config/` 并驱动引擎。
- 后端 API 不变（`/api/v1/*`）。
- 路由表以 `web/src/router/index.ts` 为唯一事实源。

---

## 3. 信息架构调整（方案）

左侧导航精简为 Accio 风格：

```
Accio Work              →  研报协作平台
新任务                  →  研报入口（/）
智能体                  →  智能体（/agents）
插件                    →  数据源（/plugins）
消息渠道                →  推送渠道（/channels）
下载桌面端              →  移除（无此功能）
─────────────────────────────────────────
新增/保留：
技能库（/skills）        →  Accio 无直接对应，可并入“插件”二级或保留为独立项
设置（/settings/*）       →  原配置控制台，建议折叠到左下角头像菜单
```

**推荐菜单结构**：
1. 新任务（/`ChatEntry`）
2. 智能体（`/agents`）
3. 插件 = 数据源（`/plugins`）
4. 技能（`/skills`）
5. 消息渠道（`/channels`）
6. 设置（折叠在左下角用户区，保留模型映射/Agent 提示词/Gate 审核/任务模板入口）

---

## 4. 页面级改动清单

### 4.1 全局布局 `DefaultLayout.vue`
- 品牌区改为 “Accio Work” 风格 Logo + 名称。
- 导航项图标与文案按新架构调整。
- 新增左下角用户区（头像 + 设置/退出）。
- 移除顶部“调试模式”开关，移到设置页或保留在右上角。
- 配色统一为白底 + `#10B981` 绿强调色（与设计系统一致）。

### 4.2 首页 `ChatEntry.vue`
- 改为聊天对话框居中布局。
- 顶部显示默认 Agent 头像 + 名称（如“研报助手”）。
- 输入框底部增加：
  - 左侧 “+ 插件” 按钮（弹出已启用的数据源/技能选择）。
  - 右侧 “切换智能体”。
- 输入框下方改为场景快捷标签：
  - 研报场景：行业研究、公司分析、竞争格局、投资风险、宏观趋势。
- 保留真实提交逻辑（调用 `buildUserTask` → `createTask` → 跳转追踪页）。

### 4.3 智能体市场 `Agents.vue`
- 卡片改为圆角、更大头像（48px 圆形或带图标）。
- 显示角色标签（如“研究员 / 分析员 / 撰稿人”）。
- 操作按钮简化为：对话（绿色）、安装/配置（灰色）。
- 删除等管理功能移到“个人” Tab 或配置抽屉内。
- 新增“公开 / 个人” Tab 已有，保留。

### 4.4 数据源 `Plugins.vue`
- 改为 Accio 插件市场样式：顶部搜索 + 分类筛选。
- 卡片显示 Logo/图标、名称、来源/官方标识、描述、+ 安装/启用。
- 已连接状态用绿色小点，即将推出置灰。
- 自定义/新增按钮保留在右上角。

### 4.5 技能库 `Skills.vue`
- 与数据源卡片样式统一。
- 增加“作用于哪些 Agent”标签。

### 4.6 消息渠道 `Channels.vue`
- 改为 Accio 大卡片布局。
- 每个渠道显示：图标、名称、未读/用户/群聊统计、连接状态。
- 操作：配对模式、设置机器人、配置智能体。
- 当前 webhook URL 配置折叠到“设置机器人”抽屉里。

### 4.7 设置入口
- 从左侧导航移除，放到左下角用户头像弹窗菜单。
- 子页：模型映射、Agent 提示词、Gate 审核、任务模板。

---

## 5. 视觉规范（可复用）

- 主色：`#10B981`
- 背景：白 `#FFFFFF`，浅灰 `#F9FAFB`
- 卡片：圆角 `12px`，阴影 `0 1px 3px rgba(0,0,0,0.06)`
- 头像：圆形，48px，使用 emoji/图标或首字母
- 字体：系统默认，标题 16-18px 中等字重，描述 13px `#6B7280`
- 按钮：主按钮绿色圆角，次按钮白色边框

---

## 6. 实施范围建议（分阶段）

**阶段 A（最小可用改法，1-2 天）**：
1. 调整左侧导航与品牌区。
2. 首页改为聊天对话框样式 + 场景标签。
3. 市场页卡片统一为圆角头像风格。

**阶段 B（完整对齐）**：
4. 消息渠道改为 Accio 大卡片。
5. 数据源/插件按分类分区展示。
6. 设置入口迁到左下角用户区。

**阶段 C（可选）**：
7. 引入 Agent 真实头像/emoji 配置。
8. 响应式/移动端适配。

---

## 7. 风险与约束

- **功能不能丢**：设置、启用/停用、连接密钥、测试推送必须保留。
- **后端不动**：仅改前端 `web/src/*` 与静态资源。
- **视觉回归**：需走 `vue-tsc --noEmit` + `npm run build` + Docker 重建验证。
- **数据展示**：当前后端 channel 数据无“用户数/群聊数/待处理数”，若要大卡片显示统计，后端需新增字段或前端用占位 + `coming_soon` 标记。

---

## 8. 验收标准

- [ ] 左侧导航与 Accio 结构一致（新任务 / 智能体 / 插件 / 消息渠道）。
- [ ] 首页为聊天对话框，输入框带插件/智能体切换 + 场景标签。
- [ ] 各市场页为圆角卡片风格。
- [ ] `vue-tsc --noEmit` EXIT=0。
- [ ] `npm run build` EXIT=0。
- [ ] Docker 重建后 `localhost:18080` 可访问，核心功能（创建任务、启用插件、配置渠道）仍可用。

---

## 9. 备注

- 当前 UI 功能完整且是 controller（非 viewer），重构是**视觉层**，不触碰后端契约。
- 若 boss 同意，按阶段 A 先落地可快速缩小与模板的视觉差距。
