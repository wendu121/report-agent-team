# VERIFICATION · ChatAgent 跨会话记忆「自我认知」补全

> 配套提交：在 `chat_agent.py` 的 `CHAT_SYSTEM_PROMPT` 增加记忆能力描述（能力条 + 专属「你的长期记忆」小节）。
> 前序根因修复：`5c138b2`（kb_store 关键词兜底召回）。

## 背景（为什么还要改）
用户实测问 ChatAgent「你有跨会话记忆吗？」，得到的是**「没有跨会话记忆」**的通用回复。
根因：**机制早已存在**（`kb_retrieve` 读 + `chat_learn_write` 写，已注入 system prompt 的
「你之前积累的相关知识」），但 `CHAT_SYSTEM_PROMPT` 从未提及该能力 → LLM 按基座知识作答 →
谎称「无记忆」。**这是诚实性问题，不是功能缺失**。本变更让 agent 如实「知道自己有记忆」。

## 变更内容（仅 prompt 文本，无可执行逻辑改动）
- `【你的能力】` 新增一条：`跨会话长期记忆` —— 告知 agent 它拥有持久化知识库、会被自动召回。
- 新增 `【你的长期记忆（跨会话知识库）】` 小节，明确三点：
  1. agent **确实**拥有跨会话持久记忆（每轮按问题召回，以「你之前积累的相关知识」注入）；
  2. 沉淀由系统自动完成（工具取证的实质回答写库；用户「记住：」是另一条需管理员采纳的经验草稿通道）；
  3. **诚实边界**：当前召回为关键词匹配（嵌入模型欠费 402，走中文 bigram + 英文词），复用关键术语召回最准，
     换说法零词面重叠可能召回不到；**严禁假装记得实际没召回到的内容，也不得在被问时否认自己有记忆**。

## 门禁 ① 构建/编译校验
- `python -m py_compile chat_agent.py` → **OK**（退出 0）。
- `docker compose build api`（清华镜像，pip 层命中缓存）→ `BUILD_EXIT=0`，已 `up -d api` 重建 `report-api`。

## 门禁 ② 自审（实测证据）
- 重建容器内重跑 `scripts/verify_memory_e2e.py` → **RESULT: PASS**
  - session1 `kb_write -> 1`（marker 写入成功）；
  - session2（空 history、换措辞「跨境电商的关税政策是怎么说的？」）标记出现在 system prompt = **True**，
    记忆标题「你之前积累的相关知识」出现 = **True**；
  - 负对照（无关问题）误注入 = **False**（符合预期）。
- `system_len` 由旧镜像 3686 → 新镜像 **4189**，增量即新 prompt 文本，证明确实烘焙进运行容器。
- 容器内 `grep -c "你的长期记忆（跨会话知识库）"` = **1**，证明确认新文本已生效。

## 诚实边界（务必如实告知 boss）
- 召回是**关键词级**（中文 bigram + 英文词），**非语义级**。语义召回依赖嵌入端点；当前 `BAAI/bge-m3`
  （SiliconFlow 渠道）余额 0 → `HTTP 402`，`embedding-3`（智谱）`429` → `/v1/embeddings` 全部不可用。
  故写入行 `embedding=NULL`，只能走关键词兜底。
- 推论：存储「关税下调 3%」、问「进口税费变化」这类**零词面重叠**的提问**召回不到**。真·语义跨会话记忆
  需：① 给嵌入渠道充值，或 ② 接免费嵌入源（如 Cloudflare `@cf/baai/bge-m3`）。当前为「不丢记忆、
  但靠关键词命中」的可用态，不是「任意说法都能想起」。

## 门禁 ③
见 `REVIEW_memory_selfdesc.md`（独立子代理产出，主代理不自签 `reviewed-by`）。
